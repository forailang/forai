# expect: ok
# stdout:
#   true
#   «secret APP_TOKEN»
#   false

use std.secrets

def main
    @return Void
do
    print(secrets.has('APP_TOKEN'))
    print(secrets.get('APP_TOKEN'))
    print(secrets.has('OPTIONAL_EXTRA'))
end
