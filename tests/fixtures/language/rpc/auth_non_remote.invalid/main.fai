# expect: check_error
# error: only valid on `remote def`

# A local function cannot carry an auth policy.
def helper
    @auth session
    @return Int
do
    1
end

def main
    @return Void
do
    print(helper())
end
