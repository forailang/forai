# expect: check_error
# error: Unknown @auth policy 'admin'

# Policy names are public or session; authorizers hang off session.
remote def ping
    @auth admin
    @return String
do
    'pong'
end

def main
    @return Void
do
    print(ping())
end
