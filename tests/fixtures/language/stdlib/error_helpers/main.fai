# expect: ok
# stdout:
#   boom
#   true
#   null

use std.error

def main
    @return Void
do
  let e = error.Error('boom')
  print(error.message(e))
  print(error.isError(e))
  print(error.kind(e))
end
