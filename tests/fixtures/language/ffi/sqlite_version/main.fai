# expect: ok
# stdout:
#   prefix=ok

use std.string

extern sqlite3
  def sqlite3_libversion() -> String
end

def main
    @return Void
do
  let v = sqlite3_libversion()
  if string.startsWith(v, '3.')
    print("prefix=ok")
  else
    print("prefix=bad: {{v}}")
  end
end
