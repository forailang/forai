# expect: ok
# stdout:
#   42
# An async closure *literal* — it suspends on `sleep`, so it compiles as a
# resume fn (env-in-frame), and the inline call is awaited via spawn-by-header.
def main
    @return Void
do
    let r = do
        sleep(5)
        42
    end()
    print(r)
end
