# expect: ok
# stdout: ok
# leak: flat

use std.json

def main
    @return Void
do
    let parsed = json.parse('{"a": 1}')
    let text = json.stringify(parsed)
    json.parse(text)
    if unwrap(getInt(parsed, 'a'), 0) == 1
        print('ok')
    end
end
