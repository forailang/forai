use { hookSemanticPages } from data.hooks

# expect: ok
# stdout:
#   1
def main
    @return Void
do
    print(length(hookSemanticPages()))
end

test main
    it 'keeps semantic pages'
        assert.equals(length(hookSemanticPages()), 1)
    end
end
