# expect: check_error
# error: Operator '+' cannot be applied to a Secret

use std.secrets

def main
    @return Void
do
    let key = secrets.get('STRIPE_KEY')
    let header = 'Bearer ' + key
    print(header)
end
