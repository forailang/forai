# expect: check_error
# error: Cannot use a Secret as a case value

use std.secrets

def main
    @return Void
do
    let key = secrets.get('STRIPE_KEY')
    case key
    when key
        print('match')
    default
        print('no match')
    end
end
