# expect: check_error
# error: Operator '==' cannot be applied to a Secret

use std.secrets

def main
    @return Void
do
    let a = secrets.get('STRIPE_KEY')
    let b = secrets.get('STRIPE_KEY')
    print(a == b)
end
