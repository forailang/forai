# expect: ok
# browser:
#   selector: #slot
#   text: Rendered in slot

use std.browser

def main
    @return Void
do
    browser.setHtml('<main><div id="slot"></div></main>')
    browser.setHtmlAt('#slot', '<section id="slot">Rendered in slot</section>')
end
