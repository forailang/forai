name: nightly

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write

env:
  CARGO_TERM_COLOR: always
  CARGO_INCREMENTAL: 0

jobs:
  test:
    name: test
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: install playwright (browser fixture harness)
        working-directory: tests/browser-harness
        run: |
          npm ci
          npx playwright install --with-deps chromium
      - name: build cli binaries (tests in fai.rs/main.rs/feature-tests invoke target/debug/{fai,forai})
        run: cargo build --locked --bin fai --bin forai
      # ffi::forsqlite_* fixtures hardcode `file:///home/bal/forai/forsqlite`
      # in their fai.toml — local dev path, not portable. Skip on CI until
      # forsqlite is referenced relatively or fetched as a sibling checkout.
      - name: run tests
        run: cargo test --workspace --locked -- --skip ffi::forsqlite_

  build:
    name: build (${{ matrix.target }})
    needs: test
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: x86_64-unknown-linux-gnu
            runner: ubuntu-24.04
            asset: fai-linux-x86_64.tar.gz
          - target: aarch64-unknown-linux-gnu
            runner: ubuntu-24.04-arm
            asset: fai-linux-aarch64.tar.gz
          - target: aarch64-apple-darwin
            runner: macos-14
            asset: fai-darwin-aarch64.tar.gz
    runs-on: ${{ matrix.runner }}
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}
      - uses: Swatinem/rust-cache@v2
        with:
          key: ${{ matrix.target }}
      - name: build fai
        run: cargo build --release --locked --bin fai --target ${{ matrix.target }}
      - name: package
        run: |
          set -euo pipefail
          bin="target/${{ matrix.target }}/release/fai"
          strip "$bin" || true
          tar -czf "${{ matrix.asset }}" -C "target/${{ matrix.target }}/release" fai
          sha256sum "${{ matrix.asset }}" > "${{ matrix.asset }}.sha256" 2>/dev/null \
            || shasum -a 256 "${{ matrix.asset }}" > "${{ matrix.asset }}.sha256"
      - uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.asset }}
          path: |
            ${{ matrix.asset }}
            ${{ matrix.asset }}.sha256

  release:
    name: publish nightly release
    needs: build
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: dist
          merge-multiple: true
      - name: list assets
        run: ls -la dist
      - uses: softprops/action-gh-release@v2
        with:
          tag_name: nightly
          name: Nightly (${{ github.sha }})
          body: |
            Rolling nightly build from `main` @ ${{ github.sha }}.
            See `install.sh` in the repo for one-line installation.
          prerelease: true
          make_latest: false
          files: |
            dist/*.tar.gz
            dist/*.sha256
